# feat/arg 対話コンテキスト

- PR: export時点でPR未作成
- Branch: `feat/arg`
- Source commit: 0106a50
- Updated at: 2026-08-25 00:00:00
- Exported by: Claude Opus 4.6

## 目的

マクロの文法を拡張して整数型の引数を受け取れるようにする。Waitの待機時間やHitコマンドの-cオプション（フレーム補正値）などを、マクロファイルを編集せずに実行時に調整できるようにすることが動機。

## 設計方針

- プレースホルダ構文として`{name}`と`{name:default}`を導入する。対応する型は整数値のみ。
- プレースホルダはコマンドの位置引数（`Hit A {frame}`）にもオプション値（`-c={correction}`）にも書ける。
- コンパイル時にパーサがプレースホルダを認識し、`MacroArg`型（リテラル値またはプレースホルダ参照を保持する判別共用体）としてコマンド内に保持する。解決は実行時に行う。この設計により、再コンパイルなしに異なる引数で再実行できる。
- `MacroArg`型はORCA.Coreに配置し、プラグインコマンドからも利用できるようにする。
- `MacroScript`は`Parameters`プロパティ（`IReadOnlyList<MacroParameter>`相当）でパラメータ一覧（名前・デフォルト値）を公開する。呼び出し側はこれを使って入力UIやプロトコルの組み立てを行う。
- `RunOnceAsync`/`RunLoopAsync`のパラメータに`Dictionary<string, int>?`を追加し、コンテキスト経由で`MacroArg`を解決する。
- `RunOnceAsync`/`RunLoopAsync`の冒頭で、最初のコマンド実行前にデフォルト値なしの全パラメータに値が渡されているか検証し、不足があれば事前条件違反として例外を投げる。
- 同名プレースホルダは複数箇所で使え、すべて同じ値で解決される。

## 却下した代替案

- テキストレベル置換（パース前に文字列置換）: 再実行時に引数だけ変えるには再コンパイルが必要になるため不採用。
- パース時解決（パーサ内で引数辞書から値を取得）: 同上の理由で不採用。
- `MacroScript.SetArguments`のような状態セッター方式: 外部から状態を注入する設計はミュータブルな状態を持ち込むため不採用。引数は`Run`メソッドのパラメータとして渡す。
- コマンドの前段に引数解決レイヤーを挟む方式: コマンドのimmutabilityを壊すか、毎回コマンドオブジェクトを再生成するコストがかかるため不採用。

## 意図的に対応しないこと

- 整数以外の型（文字列、ボタン名など）のプレースホルダ対応。用途が整数値の調整に限られるため、対応する予定がない。
- GUI/Headlessでの引数入力UIの設計。コアAPIのみを今回のスコープとする。

## 発見された制約

- 既存のコマンドパーサ（Hit, Wait, Press, Start）はint引数を直接保持しており、`MacroArg`型への差し替えが必要になる。パーサの`Parse`メソッドとコマンドクラスのコンストラクタの両方に変更が入る。
- プラグインパーサのインターフェース`IMacroCommandParser<T>`はORCA.Coreで定義されているため、`MacroArg`もORCA.Coreに置く必要がある。

## 新たに確認できた事実

- 現在のマクロシステムはGUI（Form1.cs）とHeadless（Service.cs）の両方から使われており、コアAPIの設計はどちらにも偏らない形にする必要がある。
- `IMacroContext`は実行時コンテキストとしてタイマー操作やキー・バリューストアを提供しており、引数の解決もここを経由させるのが自然。

## 注意が必要な難所

- なし

## 残作業

- `MacroArg`型の実装（ORCA.Core）
- プレースホルダのパースユーティリティの実装（ORCA.Core）
- `MacroParameter`型の実装（ORCA.Core）
- 既存コマンド（Hit, Wait, Press）のint引数を`MacroArg`に差し替え
- 既存パーサのプレースホルダ認識対応
- `MacroScript.Parameters`プロパティの追加
- `RunOnceAsync`/`RunLoopAsync`への引数パラメータ追加と検証ロジック
- `IMacroContext`への引数解決メソッド追加
- GUI/Headlessの呼び出し側の対応（引数パラメータの追加）
- テスト
